services:
  - type: web
    name: qp-wallet-service
    runtime: rust
    buildCommand: cargo build --release
    startCommand: ./target/release/qp-hd
    envVars:
      - key: PROTOCOL_SEED
        sync: false
      - key: PORT
        value: 3001